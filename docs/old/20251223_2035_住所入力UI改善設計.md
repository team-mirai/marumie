# 住所入力UI改善設計

## 概要

取引先（Counterpart）作成時の住所入力UIを改善する。現状のUIはレイアウトが複雑で主従関係がわかりにくいため、段階的開示パターンを採用してシンプルにする。

## 現状の問題点

1. 住所入力フィールドと検索UIが同時に表示され、関係性がわかりにくい
2. 検索結果をラジオボタンで選択する形式が、直接入力との関係を混乱させる
3. 「手動入力」がラジオボタンの選択肢として存在し、UIの主従が不明瞭
4. 検索パネルが大きく、フォーム全体の見通しが悪い

## 設計方針

- **段階的開示**: 初期状態ではInputを隠し、ユーザーの選択に応じて表示
- **AI検索を主導線**: ボタンを目立たせ、検索を促す
- **手動入力をサブ導線**: テキストリンクで控えめに提供
- **選択後は編集可能**: 一度選択/入力したら、通常のInputとして自由に編集可能

## UI状態遷移

### 状態1: 初期状態

```
┌─────────────────────────────────────────────────────────────────┐
│  名前 *                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 株式会社○○                                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  住所                                                           │
│                                                                 │
│  業態ヒント（任意）                                             │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 印刷                                                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────┐                                            │
│  │  🔍 AI検索       │    手動で入力する                         │
│  └─────────────────┘                                            │
└─────────────────────────────────────────────────────────────────┘
```

- 住所Inputは非表示
- 業態ヒントInputは最初から表示（任意入力）
- 「AI検索」ボタン（プライマリ）を表示
- 「手動で入力する」テキストリンクを表示

### 状態2: 検索中

```
┌─────────────────────────────────────────────────────────────────┐
│  住所                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  検索中...                                               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

- ローディング表示

### 状態3: 検索結果表示

```
┌─────────────────────────────────────────────────────────────────┐
│  住所                                                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 〒100-0001 東京都千代田区千代田1-1-1                     │   │
│  │ 根拠: 法人番号DB  高確度         [確認↗] [選択]         │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 〒100-0002 東京都千代田区丸の内2-2-2                     │   │
│  │ 根拠: Google検索  中確度         [確認↗] [選択]         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  業態ヒント（任意）                                             │
│  ┌───────────────────────────────────────┬────────┐           │
│  │ 印刷                                  │ 再検索 │           │
│  └───────────────────────────────────────┴────────┘           │
│                                                                 │
│                                          手動で入力する         │
└─────────────────────────────────────────────────────────────────┘
```

- 各候補に「選択」ボタンを配置
- 「確認↗」でGoogle検索を新しいタブで開く
- 業態ヒント入力 + 「再検索」ボタン
- 「手動で入力する」リンクも引き続き表示

### 状態4: 確定後（Input表示）

```
┌─────────────────────────────────────────────────────────────────┐
│  住所                                       [再検索] [クリア]   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 東京都千代田区千代田1-1-1                                │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

- 選択した住所がInputに反映された状態
- Inputは編集可能
- 「再検索」ボタンで状態3に戻れる
- 「クリア」ボタンで状態1に戻れる

## 状態遷移図

```
[初期状態]
    │
    ├── 「AI検索」クリック ──→ [検索中] ──→ [検索結果表示]
    │                                              │
    │                                              ├── 候補の「選択」クリック ──→ [確定後]
    │                                              │
    │                                              └── 「手動で入力する」クリック ──→ [確定後(空)]
    │
    └── 「手動で入力する」クリック ──→ [確定後(空)]

[確定後]
    │
    ├── 「再検索」クリック ──→ [検索結果表示]
    │
    └── 「クリア」クリック ──→ [初期状態]
```

## コンポーネント構成

状態管理の複雑さを考慮し、UIとロジックを分離する。

```
CounterpartFormContent.tsx（フォーム全体）
  └── AddressInput.tsx（住所入力UI）
        └── useAddressSearch.ts（住所検索ロジック）
```

### ファイル配置

```
admin/src/client/components/counterparts/
├── CounterpartFormContent.tsx  # 既存（簡素化）
├── AddressInput.tsx            # 新規：住所入力UI
└── useAddressSearch.ts         # 新規：住所検索hook
```

### useAddressSearch（カスタムhook）

住所検索の状態管理とアクションを提供する。

```typescript
type AddressInputPhase =
  | "initial"      // 初期状態（ボタンのみ）
  | "searching"    // 検索中
  | "results"      // 検索結果表示
  | "confirmed";   // 確定後（Input表示）

interface UseAddressSearchProps {
  companyName: string;
  initialAddress?: string;
}

interface UseAddressSearchReturn {
  // 状態
  phase: AddressInputPhase;
  address: string;
  searchResult: SearchResult | null;
  hint: string;
  isSearching: boolean;

  // アクション
  startSearch: () => void;
  reSearch: () => void;
  selectCandidate: (candidate: AddressCandidate) => void;
  switchToManual: () => void;
  clear: () => void;
  setAddress: (value: string) => void;
  setHint: (value: string) => void;
}

function useAddressSearch(props: UseAddressSearchProps): UseAddressSearchReturn;
```

### AddressInput（UIコンポーネント）

phaseに応じたUIを描画する。ロジックはhookに委譲し、純粋なレンダリングに専念する。

```typescript
interface AddressInputProps {
  companyName: string;
  address: string;
  onChange: (address: string) => void;
  disabled?: boolean;
}

function AddressInput(props: AddressInputProps): JSX.Element;
```

### CounterpartFormContent（既存の簡素化）

住所検索ロジックをAddressInputに移譲することで、フォーム全体の見通しが良くなる。

```tsx
// Before: 住所検索ロジックが混在（347行）
// After: AddressInputを利用するだけ

<AddressInput
  companyName={name}
  address={address}
  onChange={setAddress}
  disabled={isDisabled}
/>
```

## 分離のメリット

1. **テスト容易性**: useAddressSearchを単体テストできる
2. **責務分離**: UIはレンダリング、hookはロジックに専念
3. **可読性**: CounterpartFormContentがシンプルになる
4. **再利用性**: 他の場所で住所検索が必要になった場合に再利用可能

## 初期値の設定

紐付けダイアログから新規作成する場合、Transactionの摘要（description）を名前の初期値として使用する。

### CounterpartFormContentのProps拡張

```typescript
interface CounterpartFormContentProps {
  mode: "create" | "edit";
  initialData?: {
    id: string;
    name: string;
    address: string | null;
    usageCount?: number;
  };
  defaultName?: string;  // 追加：摘要からの初期値
  onSubmit: (data: { name: string; address: string | null }) => Promise<void>;
  disabled?: boolean;
}
```

### AssignWithCounterpartContentからの呼び出し

```tsx
<CounterpartFormContent
  mode="create"
  defaultName={transactions[0]?.description ?? ""}
  onSubmit={handleCreateAndAssign}
  disabled={isPending}
/>
```

### 動作

- `defaultName`が渡された場合、名前フィールドの初期値として使用
- ユーザーは自由に編集可能
- 摘要が空の場合は空文字列
